# Legacy transcription scripts

Superseded by `src/transcribe.py` (unified CLI) and the `src/transcription/` package.
Kept for historical reference only — no bugfixes, no new features.

If `src/transcribe.py` grows a regression, cross-reference these to see how the
original one-off variant handled the same case.
