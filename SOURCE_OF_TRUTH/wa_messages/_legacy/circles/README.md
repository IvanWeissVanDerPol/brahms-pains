# circles/ — Empty legacy folder

This folder was moved from `wa_messages/circles/` to `_legacy/circles/` on 2026-07-25.

**Why**: The 5 subdirectories (`family_weiss_vdp`, `fpuna_cs_classmates`, etc.) contain NO `messages.json` files — they're empty placeholders for Google Circles data that was never populated.

**Original purpose**: Import from Google Contacts Circles (legacy social network feature, deprecated since 2019).

**Action**: If you need to recover circle data, it's in the deleted `.opencode/` or backup system. Otherwise this folder can be deleted entirely.
